# Components

SQLite Explorer currently keeps app-specific components inside `src/index.template.html` so the release remains easy to audit.
Reusable components may be added here when they are shared with other htmlapps repositories.
